#include <stdio.h>
#include "decoder.h"

uint32_t decode_instruction(uint32_t instruction_word) {
    // TODO: Richard will implement the actual decoding logic here.
    // For now, it simply returns the instruction word itself.
    // In a real scenario, this would populate a DecodedInstruction struct.
    return instruction_word;
}